# Dependencies
node_modules/

# Build output
dist/
build/
*.zip
# Unzipped release package (transient — the extracted form of prompt-vault-v*.zip)
prompt-vault-v*/

# Env / secrets
.env
.env.local

# Editor / OS
.DS_Store
*.log

# Playwright MCP scratch (snapshots, console logs — fixture capture only)
.playwright-mcp/

# Harness scratch (delegation evidence, session tmp)
.claude/tmp/

# Harness maintenance stamp (transient date marker, not project state)
.agents/.maintenance-stamp

# Active sprint file (transient — exists only during a sprint)
# tasks.md is intentionally tracked when present; do not ignore.

# repo-quiz local progress (personal, not shared)
.repo-quiz/
