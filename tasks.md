## Review Backlog

### Bulk panel "Load more" follow-up

- [ ] [VERIFY] Project home page (`/g/g-p-…/project`) live check: confirm `findScrollableAncestor(projectListSection)` targets the real scroll container, `.text-sm.font-medium` titles extract, and the bulk list surfaces the full project conversation set. The history-sidebar analog was verified live 2026-07-22 (`#history` → `NAV.group/scrollport overflow-y-auto`, scrollH>clientH; the accumulate-across-scroll-rounds walk captured the full 19-row list and settled into the done state; aria-label titles present synchronously at first sight). The project-home path was not exercised this session. Deferred — needs a logged-in session with at least one Project.
- [ ] [VERIFY] Recycling-virtualizer live path AND rendered bulk-panel UI: on a history sidebar / project list long enough to *window* (recycle a fixed node pool), confirm `loadMoreConversations` / `loadMoreProjectConversations` accumulate rows the virtualizer trims off the top, and the loaded extension's bulk panel settles into its disabled done state with prior selections preserved. Unit-covered by the spacer-height recycling model in `test/adapters/chatgpt/load-more.test.ts`; the accumulate-across-rounds algorithm was verified live 2026-07-22 but only on a 19-item account that never windows (all nodes stay in the DOM), and the packed-extension panel UI itself was not exercised (MCP cannot load the unpacked extension). Deferred — needs a logged-in session with a many-hundred-item history AND the unpacked extension loaded.
